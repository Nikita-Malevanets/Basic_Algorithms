📌 Завдання 2. Порівняння алгоритмів DFS та BFS
Опис

У даному завданні реалізовано алгоритми DFS (Depth-First Search) та BFS (Breadth-First Search) для знаходження шляхів у графі, що моделює транспортну мережу міст України.

Метою є порівняння результатів роботи обох алгоритмів та пояснення різниці у знайдених шляхах.

Використаний граф

Алгоритми застосовувалися до одного й того ж неорієнтованого графа.
Шлях шукався між містами Lviv та Odesa.

Результати роботи алгоритмів

BFS path:

Lviv → Kyiv → Odesa


DFS path:

Lviv → Vinnytsia → Dnipro → Kharkiv → Poltava → Kyiv → Odesa

Пояснення отриманих результатів

Алгоритм BFS виконує пошук у ширину, поступово перевіряючи всі вершини одного рівня перед переходом до наступного.
Завдяки цьому BFS гарантує знаходження найкоротшого шляху за кількістю ребер у неваговому графі. У даному випадку таким шляхом є маршрут через місто Kyiv.

Алгоритм DFS, навпаки, заглиблюється в першу доступну гілку графа та повертає перший знайдений шлях, не аналізуючи всі можливі альтернативи.
Отриманий шлях залежить від порядку сусідів у списку суміжності та може бути значно довшим, ніж оптимальний.

Висновок

BFS доцільно використовувати для пошуку найкоротших шляхів у невагових графах.

DFS корисний для обходу графа та перевірки досяжності вершин, але не гарантує оптимальність шляху.

Навіть на простому графі різниця між алгоритмами є наочною та зрозумілою.